// import Vue from 'vue'

export default {

  /**
   *
   * @param {
   * add_to_cart,
   * login,
   * sign_up
   * purchase,
   * search,
   * share
   * } type
   * @param {*} event
   */
  // registerEvent (type, event) {
  //   $gtm('event', type, event)
  // }
}
